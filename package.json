{
	"name": "raydb",
	"private": true,
	"description": "RayDB monorepo",
	"workspaces": ["ray-rs"],
	"scripts": {
		"build": "npm run build --prefix ray-rs",
		"test": "npm run test --prefix ray-rs",
		"docs:dev": "npm run dev --prefix ray-docs",
		"docs:build": "npm run build --prefix ray-docs"
	}
}
