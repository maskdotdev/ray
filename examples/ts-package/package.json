{
  "name": "kitedb-ts-example",
  "private": true,
  "version": "0.0.3",
  "description": "TypeScript example using the published @kitedb/core package",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@kitedb/core": "0.2.13"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.9.2"
  }
}
